<Page x:Class="LLMockApiClient.Pages.ServerLogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="900" d:DesignWidth="1200"
      Title="Server Logs">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="📜 Server Logs" FontSize="32" FontWeight="SemiBold" Margin="0,0,0,8" />
            <TextBlock Text="ASP.NET Core server logs (Information level and above)" FontSize="16" Opacity="0.7" />
        </StackPanel>

        <!-- Controls -->
        <Border Grid.Row="1" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Server Status:" Margin="0,0,12,0" VerticalAlignment="Center" />
                    <TextBlock x:Name="ServerStatusText" FontWeight="SemiBold" VerticalAlignment="Center" />

                    <TextBlock Text="Log Count:" Margin="24,0,12,0" VerticalAlignment="Center" />
                    <TextBlock x:Name="LogCountText" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>

                <CheckBox Grid.Column="1" x:Name="AutoScrollCheckBox" Content="Auto-scroll"
                          IsChecked="True" Margin="0,0,16,0" VerticalAlignment="Center" />

                <Button Grid.Column="2" Content="🗑️ Clear Logs" Click="ClearLogs_Click" Padding="16,8" />
            </Grid>
        </Border>

        <!-- Log Display -->
        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="8" Padding="0">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="LogsItemsControl" Padding="16">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#333333" Padding="0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}"
                                               FontFamily="Consolas" FontSize="11"
                                               Foreground="#888888" />

                                    <Border Grid.Column="1" CornerRadius="4" Padding="4,2"
                                            Background="{Binding LevelColor}" HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding Level}"
                                                   FontFamily="Consolas" FontSize="10" FontWeight="SemiBold"
                                                   Foreground="White" />
                                    </Border>

                                    <StackPanel Grid.Column="2" Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Category}"
                                                   FontFamily="Consolas" FontSize="10"
                                                   Foreground="#61AFEF" Margin="0,0,0,2" />
                                        <TextBlock Text="{Binding Message}"
                                                   FontFamily="Consolas" FontSize="11"
                                                   Foreground="#D4D4D4" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Exception}"
                                                   FontFamily="Consolas" FontSize="10"
                                                   Foreground="#E06C75" TextWrapping="Wrap"
                                                   Margin="0,4,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>